import 'package:flutter/material.dart';

class Pantalla2_0412 extends StatelessWidget {
  const Pantalla2_0412({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Pantalla 2 Sandoval0412"),
        backgroundColor: Colors.blueGrey,
      ),
      body: Center(
        child: Column(mainAxisAlignment: MainAxisAlignment.center, children: [
          ElevatedButton(
            onPressed: () {},
            child: Text("Pantalla"),
          )
        ]),
      ),
    );
  }
}
